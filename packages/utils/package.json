{
  "name": "@pokedox/utils",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": false,
  "license": "MIT",
  "scripts": {
    "build": "tsc --project tsconfig.json && tsc --project tsconfig.json --module commonjs",
    "test": "jest --config jest.config.js",
    "coverage": "jest --config jest.config.js --coverage"
  }
}
